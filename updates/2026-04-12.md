# 2026-04-12 변경 내용

## 구글 로그인 기능 추가

### 1. Backend: Google ID 토큰 검증 라이브러리
**파일:** `backend/src/lib/auth.ts` (신규), `backend/package.json`

- `google-auth-library` 패키지 추가
- `verifyGoogleToken(authHeader)` 함수: `Authorization: Bearer <token>` 헤더에서 Google ID 토큰을 검증, `{ userId, email }` 반환

---

### 2. Backend: 업로드 엔드포인트 인증 보호
**파일:** `backend/src/handlers/getUploadUrl.ts`, `backend/src/handlers/createCat.ts`

- 두 핸들러 모두 인증 없으면 401 반환
- `createCat`은 `userId` (Google `sub` 클레임)를 DynamoDB 레코드에 저장

---

### 3. Backend: CatItem 타입 업데이트
**파일:** `backend/src/types/index.ts`

- `CatItem`에 `userId: string` 필드 추가

---

### 4. Frontend: AuthContext
**파일:** `frontend/src/contexts/AuthContext.tsx` (신규)

- `user` 상태 (credential, name, email, picture)
- `login()` / `logout()` 메서드

---

### 5. Frontend: LoginButton 컴포넌트
**파일:** `frontend/src/components/Auth/LoginButton.tsx` (신규)

- 로그인 전: Google Sign-In 버튼 (`@react-oauth/google`)
- 로그인 후: 프로필 사진 + 이름 + Logout 버튼

---

### 6. Frontend: Provider 추가
**파일:** `frontend/src/main.tsx`

- `GoogleOAuthProvider` (clientId: `VITE_GOOGLE_CLIENT_ID`)
- `AuthProvider` 래핑

---

### 7. Frontend: 헤더 통합 및 업로드 보호
**파일:** `frontend/src/App.tsx`, `frontend/src/hooks/useUpload.ts`, `frontend/src/api/client.ts`

- 헤더에 `LoginButton` 표시
- "Spot a Cat" 버튼은 로그인 시에만 노출
- `getUploadUrl`, `createCat` API 호출에 `Authorization: Bearer <token>` 헤더 추가
- 미로그인 상태에서 submit 시도 시 에러 처리

---

### 8. Infrastructure: google_client_id 변수 추가
**파일:** `infrastructure/variables.tf`, `infrastructure/modules/api/variables.tf`, `infrastructure/modules/api/main.tf`, `infrastructure/main.tf`

- `google_client_id` 변수 추가
- Lambda 환경변수 `GOOGLE_CLIENT_ID` 추가
- API Gateway CORS `allow_headers`에 `Authorization` 추가

---

## 배포 전 필요한 설정

1. **Google Cloud Console**에서 OAuth 2.0 Web Client ID 생성
   - Authorized JavaScript origins: 프론트 도메인 + `http://localhost:5173`
   - Authorized redirect URIs: 불필요 (popup 방식)

2. **프론트엔드** `.env` 파일에 추가:
   ```
   VITE_GOOGLE_CLIENT_ID=<your-client-id>.apps.googleusercontent.com
   ```

3. **Terraform** 배포 시:
   ```
   TF_VAR_google_client_id=<your-client-id>.apps.googleusercontent.com terraform apply
   ```

4. 백엔드 빌드 후 `terraform apply`

---

## 커밋 메시지 작성 팁 (시니어 스타일)

- **왜(why)를 설명** — 무엇을 바꿨는지보다 왜 바꿨는지를 본문에 담는다
  - 나쁜 예: `fix login bug`
  - 좋은 예: `fix: persist auth session across page refreshes`
- **부작용(side effect)도 명시** — 변경으로 인해 같이 처리되는 동작을 설명
  - 예: "`exp` 검증으로 만료된 토큰이 자동으로 정리된다"
- **제목은 50자 이내, 본문은 72자 줄바꿈** — 터미널/GitHub에서 잘 읽힘
- **Conventional Commits 형식 사용** — `feat:`, `fix:`, `refactor:`, `chore:`, `docs:` 등
  - 자동 changelog 생성 도구와 호환됨
- **커밋 하나 = 하나의 논리적 변경** — 리뷰어가 diff 읽을 때 맥락이 명확해짐
  - 여러 버그를 고쳤다면 커밋도 나눠서

---
---

# 2026-04-12 Changes (English)

## Google Login Feature

### 1. Backend: Google ID Token Verification Library
**Files:** `backend/src/lib/auth.ts` (new), `backend/package.json`

- Added `google-auth-library` package
- `verifyGoogleToken(authHeader)`: verifies a Google ID token from the `Authorization: Bearer <token>` header, returns `{ userId, email }`

---

### 2. Backend: Protect Upload Endpoints
**Files:** `backend/src/handlers/getUploadUrl.ts`, `backend/src/handlers/createCat.ts`

- Both handlers return 401 if no valid token is provided
- `createCat` now stores `userId` (Google `sub` claim) in the DynamoDB record

---

### 3. Backend: CatItem Type Update
**File:** `backend/src/types/index.ts`

- Added `userId: string` field to `CatItem`

---

### 4. Frontend: AuthContext
**File:** `frontend/src/contexts/AuthContext.tsx` (new)

- Holds `user` state (credential, name, email, picture)
- Persists session to `localStorage`; evicts tokens whose `exp` has passed on mount
- Exposes `login()` / `logout()` methods

---

### 5. Frontend: LoginButton Component
**File:** `frontend/src/components/Auth/LoginButton.tsx` (new)

- Signed out: renders Google Sign-In button via `@react-oauth/google`
- Signed in: shows profile picture, display name, and a Logout button

---

### 6. Frontend: Provider Setup
**File:** `frontend/src/main.tsx`

- Wrapped app with `GoogleOAuthProvider` (reads `VITE_GOOGLE_CLIENT_ID`)
- Wrapped app with `AuthProvider`

---

### 7. Frontend: Header Integration & Upload Guard
**Files:** `frontend/src/App.tsx`, `frontend/src/hooks/useUpload.ts`, `frontend/src/api/client.ts`

- `LoginButton` added to the header
- "Spot a Cat" button is only rendered when the user is signed in
- `getUploadUrl` and `createCat` now send `Authorization: Bearer <token>`
- `submit` short-circuits with an error message if called while signed out

---

### 8. Infrastructure: google_client_id Variable
**Files:** `infrastructure/variables.tf`, `infrastructure/modules/api/variables.tf`, `infrastructure/modules/api/main.tf`, `infrastructure/main.tf`

- Added `google_client_id` Terraform variable
- Injected as `GOOGLE_CLIENT_ID` Lambda environment variable
- Added `Authorization` to API Gateway CORS `allow_headers`

---

## Commit Message Tips (Senior Dev Style)

- **Explain the why, not just the what** — the body should answer "why was this necessary?"
  - Bad: `fix login bug`
  - Good: `fix: persist auth session across page refreshes`
- **Call out side effects** — mention behaviour that changes as a consequence
  - e.g. "expired tokens are evicted automatically via `exp` validation"
- **Subject ≤ 50 chars, body wrapped at 72** — readable in terminals and GitHub
- **Follow Conventional Commits** — `feat:`, `fix:`, `refactor:`, `chore:`, `docs:`
  - Enables automated changelog generation
- **One commit = one logical change** — makes reviews and bisects easier
  - If you fixed multiple bugs, split them into separate commits

---
---

# 2026-04-12 추가 변경 (세션 2)

## 1. 지도 클릭 → 업로드 기능
**파일:** `LocationPicker.tsx`, `SingaporeMap.tsx`, `hooks/useUpload.ts`, `App.tsx`

- 로그인한 유저가 지도를 클릭하면 업로드 모달이 열리고 해당 좌표가 자동 입력됨
- 좌표가 미리 설정된 경우 위치 선택 단계를 건너뛰고 바로 상세 입력으로 이동
- 로그인 시 지도 커서가 pointer로 변경됨

## 2. 백엔드 유닛 테스트 (Vitest) — 33개
**파일:** `backend/src/__tests__/`

- `lib/response.test.ts` — 응답 헬퍼, 보안 헤더, CORS
- `lib/auth.test.ts` — Google 토큰 검증 케이스 (유효/만료/누락/sub 없음)
- `handlers/deleteCat.test.ts` — admin key 검증, soft delete
- `handlers/getCats.test.ts` — cursor 스키마 화이트리스트, GSI 사용 확인
- `handlers/createCat.test.ts` — 싱가포르 좌표 검증, 인증, 제목 trim
- `handlers/getUploadUrl.test.ts` — 파일 타입/크기 검증, 인증

## 3. 프론트엔드 유닛 테스트 (Vitest + RTL) — 13개
**파일:** `frontend/src/__tests__/`

- `AuthContext.test.tsx` — 로그인/로그아웃, localStorage 저장, 만료 토큰 자동 제거
- `useUpload.test.ts` — 상태 머신 전환, 지도 클릭 후 위치 건너뜀 확인

## 4. E2E 테스트 (Playwright)
**파일:** `e2e/`, `playwright.config.ts`

- `auth.spec.ts` — 미로그인 시 버튼 숨김, 로그인 상태 확인, 로그아웃
- `map.spec.ts` — 지도 렌더링, 로그인 시 클릭→모달 오픈, 미로그인 시 차단
- `e2e/helpers/auth.ts` — localStorage에 가짜 토큰 주입 (OAuth 팝업 우회)

## 5. CI/CD (GitHub Actions + AWS OIDC)
**파일:** `.github/workflows/`, `infrastructure/modules/github_oidc/`

### `ci.yml`
- PR/push마다: typecheck → unit test (백엔드 + 프론트)
- main 머지 시: E2E 추가 실행

### `deploy.yml`
- main 머지 시 자동 배포: Lambda 업데이트 → S3 sync → CloudFront 무효화
- **AWS OIDC 연동**: `AWS_ACCESS_KEY_ID` 없이 IAM Role 직접 assume
- `index.html`은 `no-cache`, 나머지 assets는 `immutable` (캐시 최적화)

### Terraform: `modules/github_oidc/`
- GitHub Actions OIDC Provider 생성
- 이 레포에만 허용되는 최소 권한 IAM Role
- `terraform apply` 후 출력되는 `github_actions_role_arn`을 GitHub Secrets의 `AWS_DEPLOY_ROLE_ARN`에 설정

---

## GitHub Secrets 설정 목록

```
AWS_DEPLOY_ROLE_ARN        # terraform output github_actions_role_arn
FRONTEND_BUCKET            # hdb-cats-frontend-prod
CLOUDFRONT_DISTRIBUTION_ID # E1L7ZSNATVUR9R
VITE_API_BASE_URL          # https://cym7448p6e.execute-api.ap-southeast-1.amazonaws.com/v1
VITE_CLOUDFRONT_DOMAIN     # https://d3gi18y8aq6jdp.cloudfront.net
VITE_GOOGLE_CLIENT_ID      # 374175542350-adnnouk6ku2rnibor99fslt99i18vbn7.apps.googleusercontent.com
```

---
---

# 2026-04-12 Additional Changes (Session 2) — English

## 1. Map-click to upload
**Files:** `LocationPicker.tsx`, `SingaporeMap.tsx`, `hooks/useUpload.ts`, `App.tsx`

- Authenticated users can click anywhere on the map to open the upload modal with coordinates pre-filled
- The location-picking step is skipped when a location is already set
- Map cursor changes to `pointer` when a user is logged in

## 2. Backend unit tests (Vitest) — 33 tests
**Files:** `backend/src/__tests__/`

- Response helpers, security headers, CORS
- Google token verification (valid / expired / missing / no sub claim)
- Admin key guard and soft-delete in deleteCat
- Cursor schema whitelist and GSI usage in getCats
- Singapore bounds validation, auth check, title trimming in createCat
- File type / size validation and auth check in getUploadUrl

## 3. Frontend unit tests (Vitest + RTL) — 13 tests
**Files:** `frontend/src/__tests__/`

- AuthContext: login, logout, localStorage persistence, expired-token eviction
- useUpload state machine: all transitions including the new map-click shortcut

## 4. E2E tests (Playwright)
**Files:** `e2e/`, `playwright.config.ts`

- auth.spec: sign-in button hidden when logged out, visible when logged in, logout clears storage
- map.spec: map renders, click-to-upload works when authed, is blocked when logged out
- Fake auth helper injects a structurally-valid JWT directly into localStorage — no real Google OAuth popup needed in CI

## 5. CI/CD (GitHub Actions + AWS OIDC)
**Files:** `.github/workflows/`, `infrastructure/modules/github_oidc/`

### `ci.yml`
- Every PR / push: typecheck → unit tests (backend + frontend)
- Push to main: additionally runs the Playwright E2E suite

### `deploy.yml`
- Auto-deploys on every merge to main: Lambda code update → S3 sync → CloudFront invalidation
- **AWS OIDC**: no long-lived `AWS_ACCESS_KEY_ID` in Secrets — uses `sts:AssumeRoleWithWebIdentity`
- `index.html` served with `no-cache`; hashed assets served with `immutable` for optimal CDN caching

### Terraform: `modules/github_oidc/`
- Creates the GitHub Actions OIDC provider in AWS
- Least-privilege IAM role scoped to this repo only
- Run `terraform apply` and copy the `github_actions_role_arn` output into `AWS_DEPLOY_ROLE_ARN` in GitHub Secrets
