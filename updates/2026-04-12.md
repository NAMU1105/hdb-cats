# 2026-04-12 변경 내용

## 구글 로그인 기능 추가

### 1. Backend: Google ID 토큰 검증 라이브러리

**파일:** `backend/src/lib/auth.ts` (신규), `backend/package.json`

- `google-auth-library` 패키지 추가
- `verifyGoogleToken(authHeader)` 함수: `Authorization: Bearer <token>` 헤더에서 Google ID 토큰을 검증, `{ userId, email }` 반환

---

### 2. Backend: 업로드 엔드포인트 인증 보호

**파일:** `backend/src/handlers/getUploadUrl.ts`, `backend/src/handlers/createCat.ts`

- 두 핸들러 모두 인증 없으면 401 반환
- `createCat`은 `userId` (Google `sub` 클레임)를 DynamoDB 레코드에 저장

---

### 3. Backend: CatItem 타입 업데이트

**파일:** `backend/src/types/index.ts`

- `CatItem`에 `userId: string` 필드 추가

---

### 4. Frontend: AuthContext

**파일:** `frontend/src/contexts/AuthContext.tsx` (신규)

- `user` 상태 (credential, name, email, picture)
- `login()` / `logout()` 메서드

---

### 5. Frontend: LoginButton 컴포넌트

**파일:** `frontend/src/components/Auth/LoginButton.tsx` (신규)

- 로그인 전: Google Sign-In 버튼 (`@react-oauth/google`)
- 로그인 후: 프로필 사진 + 이름 + Logout 버튼

---

### 6. Frontend: Provider 추가

**파일:** `frontend/src/main.tsx`

- `GoogleOAuthProvider` (clientId: `VITE_GOOGLE_CLIENT_ID`)
- `AuthProvider` 래핑

---

### 7. Frontend: 헤더 통합 및 업로드 보호

**파일:** `frontend/src/App.tsx`, `frontend/src/hooks/useUpload.ts`, `frontend/src/api/client.ts`

- 헤더에 `LoginButton` 표시
- "Spot a Cat" 버튼은 로그인 시에만 노출
- `getUploadUrl`, `createCat` API 호출에 `Authorization: Bearer <token>` 헤더 추가
- 미로그인 상태에서 submit 시도 시 에러 처리

---

### 8. Infrastructure: google_client_id 변수 추가

**파일:** `infrastructure/variables.tf`, `infrastructure/modules/api/variables.tf`, `infrastructure/modules/api/main.tf`, `infrastructure/main.tf`

- `google_client_id` 변수 추가
- Lambda 환경변수 `GOOGLE_CLIENT_ID` 추가
- API Gateway CORS `allow_headers`에 `Authorization` 추가

---

## 배포 전 필요한 설정

1. **Google Cloud Console**에서 OAuth 2.0 Web Client ID 생성
   - Authorized JavaScript origins: 프론트 도메인 + `http://localhost:5173`
   - Authorized redirect URIs: 불필요 (popup 방식)

2. **프론트엔드** `.env` 파일에 추가:

   ```
   VITE_GOOGLE_CLIENT_ID=<your-client-id>.apps.googleusercontent.com
   ```

3. **Terraform** 배포 시:

   ```
   TF_VAR_google_client_id=<your-client-id>.apps.googleusercontent.com terraform apply
   ```

4. 백엔드 빌드 후 `terraform apply`

---

## 커밋 메시지 작성 팁 (시니어 스타일)

- **왜(why)를 설명** — 무엇을 바꿨는지보다 왜 바꿨는지를 본문에 담는다
  - 나쁜 예: `fix login bug`
  - 좋은 예: `fix: persist auth session across page refreshes`
- **부작용(side effect)도 명시** — 변경으로 인해 같이 처리되는 동작을 설명
  - 예: "`exp` 검증으로 만료된 토큰이 자동으로 정리된다"
- **제목은 50자 이내, 본문은 72자 줄바꿈** — 터미널/GitHub에서 잘 읽힘
- **Conventional Commits 형식 사용** — `feat:`, `fix:`, `refactor:`, `chore:`, `docs:` 등
  - 자동 changelog 생성 도구와 호환됨
- **커밋 하나 = 하나의 논리적 변경** — 리뷰어가 diff 읽을 때 맥락이 명확해짐
  - 여러 버그를 고쳤다면 커밋도 나눠서

---

---

# 2026-04-12 Changes (English)

## Google Login Feature

### 1. Backend: Google ID Token Verification Library

**Files:** `backend/src/lib/auth.ts` (new), `backend/package.json`

- Added `google-auth-library` package
- `verifyGoogleToken(authHeader)`: verifies a Google ID token from the `Authorization: Bearer <token>` header, returns `{ userId, email }`

---

### 2. Backend: Protect Upload Endpoints

**Files:** `backend/src/handlers/getUploadUrl.ts`, `backend/src/handlers/createCat.ts`

- Both handlers return 401 if no valid token is provided
- `createCat` now stores `userId` (Google `sub` claim) in the DynamoDB record

---

### 3. Backend: CatItem Type Update

**File:** `backend/src/types/index.ts`

- Added `userId: string` field to `CatItem`

---

### 4. Frontend: AuthContext

**File:** `frontend/src/contexts/AuthContext.tsx` (new)

- Holds `user` state (credential, name, email, picture)
- Persists session to `localStorage`; evicts tokens whose `exp` has passed on mount
- Exposes `login()` / `logout()` methods

---

### 5. Frontend: LoginButton Component

**File:** `frontend/src/components/Auth/LoginButton.tsx` (new)

- Signed out: renders Google Sign-In button via `@react-oauth/google`
- Signed in: shows profile picture, display name, and a Logout button

---

### 6. Frontend: Provider Setup

**File:** `frontend/src/main.tsx`

- Wrapped app with `GoogleOAuthProvider` (reads `VITE_GOOGLE_CLIENT_ID`)
- Wrapped app with `AuthProvider`

---

### 7. Frontend: Header Integration & Upload Guard

**Files:** `frontend/src/App.tsx`, `frontend/src/hooks/useUpload.ts`, `frontend/src/api/client.ts`

- `LoginButton` added to the header
- "Spot a Cat" button is only rendered when the user is signed in
- `getUploadUrl` and `createCat` now send `Authorization: Bearer <token>`
- `submit` short-circuits with an error message if called while signed out

---

### 8. Infrastructure: google_client_id Variable

**Files:** `infrastructure/variables.tf`, `infrastructure/modules/api/variables.tf`, `infrastructure/modules/api/main.tf`, `infrastructure/main.tf`

- Added `google_client_id` Terraform variable
- Injected as `GOOGLE_CLIENT_ID` Lambda environment variable
- Added `Authorization` to API Gateway CORS `allow_headers`

---

## Commit Message Tips (Senior Dev Style)

- **Explain the why, not just the what** — the body should answer "why was this necessary?"
  - Bad: `fix login bug`
  - Good: `fix: persist auth session across page refreshes`
- **Call out side effects** — mention behaviour that changes as a consequence
  - e.g. "expired tokens are evicted automatically via `exp` validation"
- **Subject ≤ 50 chars, body wrapped at 72** — readable in terminals and GitHub
- **Follow Conventional Commits** — `feat:`, `fix:`, `refactor:`, `chore:`, `docs:`
  - Enables automated changelog generation
- **One commit = one logical change** — makes reviews and bisects easier
  - If you fixed multiple bugs, split them into separate commits

---

---

# 2026-04-12 추가 변경 (세션 2)

## 1. 지도 클릭 → 업로드 기능

**파일:** `LocationPicker.tsx`, `SingaporeMap.tsx`, `hooks/useUpload.ts`, `App.tsx`

- 로그인한 유저가 지도를 클릭하면 업로드 모달이 열리고 해당 좌표가 자동 입력됨
- 좌표가 미리 설정된 경우 위치 선택 단계를 건너뛰고 바로 상세 입력으로 이동
- 로그인 시 지도 커서가 pointer로 변경됨

## 2. 백엔드 유닛 테스트 (Vitest) — 33개

**파일:** `backend/src/__tests__/`

- `lib/response.test.ts` — 응답 헬퍼, 보안 헤더, CORS
- `lib/auth.test.ts` — Google 토큰 검증 케이스 (유효/만료/누락/sub 없음)
- `handlers/deleteCat.test.ts` — admin key 검증, soft delete
- `handlers/getCats.test.ts` — cursor 스키마 화이트리스트, GSI 사용 확인
- `handlers/createCat.test.ts` — 싱가포르 좌표 검증, 인증, 제목 trim
- `handlers/getUploadUrl.test.ts` — 파일 타입/크기 검증, 인증

## 3. 프론트엔드 유닛 테스트 (Vitest + RTL) — 13개

**파일:** `frontend/src/__tests__/`

- `AuthContext.test.tsx` — 로그인/로그아웃, localStorage 저장, 만료 토큰 자동 제거
- `useUpload.test.ts` — 상태 머신 전환, 지도 클릭 후 위치 건너뜀 확인

## 4. E2E 테스트 (Playwright)

**파일:** `e2e/`, `playwright.config.ts`

- `auth.spec.ts` — 미로그인 시 버튼 숨김, 로그인 상태 확인, 로그아웃
- `map.spec.ts` — 지도 렌더링, 로그인 시 클릭→모달 오픈, 미로그인 시 차단
- `e2e/helpers/auth.ts` — localStorage에 가짜 토큰 주입 (OAuth 팝업 우회)

## 5. CI/CD (GitHub Actions + AWS OIDC)

**파일:** `.github/workflows/`, `infrastructure/modules/github_oidc/`

### `ci.yml`

- PR/push마다: typecheck → unit test (백엔드 + 프론트)
- main 머지 시: E2E 추가 실행

### `deploy.yml`

- main 머지 시 자동 배포: Lambda 업데이트 → S3 sync → CloudFront 무효화
- **AWS OIDC 연동**: `AWS_ACCESS_KEY_ID` 없이 IAM Role 직접 assume
- `index.html`은 `no-cache`, 나머지 assets는 `immutable` (캐시 최적화)

### Terraform: `modules/github_oidc/`

- GitHub Actions OIDC Provider 생성
- 이 레포에만 허용되는 최소 권한 IAM Role
- `terraform apply` 후 출력되는 `github_actions_role_arn`을 GitHub Secrets의 `AWS_DEPLOY_ROLE_ARN`에 설정

---

## GitHub Secrets 설정 목록

```
AWS_DEPLOY_ROLE_ARN        # terraform output github_actions_role_arn
FRONTEND_BUCKET            # hdb-cats-frontend-prod
CLOUDFRONT_DISTRIBUTION_ID # terraform output frontend_distribution_id
VITE_API_BASE_URL          # terraform output api_base_url
VITE_CLOUDFRONT_DOMAIN     # terraform output cloudfront_domain
VITE_GOOGLE_CLIENT_ID      # <google-oauth-client-id>
```

---

---

# 2026-04-12 Additional Changes (Session 2) — English

## 1. Map-click to upload

**Files:** `LocationPicker.tsx`, `SingaporeMap.tsx`, `hooks/useUpload.ts`, `App.tsx`

- Authenticated users can click anywhere on the map to open the upload modal with coordinates pre-filled
- The location-picking step is skipped when a location is already set
- Map cursor changes to `pointer` when a user is logged in

## 2. Backend unit tests (Vitest) — 33 tests

**Files:** `backend/src/__tests__/`

- Response helpers, security headers, CORS
- Google token verification (valid / expired / missing / no sub claim)
- Admin key guard and soft-delete in deleteCat
- Cursor schema whitelist and GSI usage in getCats
- Singapore bounds validation, auth check, title trimming in createCat
- File type / size validation and auth check in getUploadUrl

## 3. Frontend unit tests (Vitest + RTL) — 13 tests

**Files:** `frontend/src/__tests__/`

- AuthContext: login, logout, localStorage persistence, expired-token eviction
- useUpload state machine: all transitions including the new map-click shortcut

## 4. E2E tests (Playwright)

**Files:** `e2e/`, `playwright.config.ts`

- auth.spec: sign-in button hidden when logged out, visible when logged in, logout clears storage
- map.spec: map renders, click-to-upload works when authed, is blocked when logged out
- Fake auth helper injects a structurally-valid JWT directly into localStorage — no real Google OAuth popup needed in CI

## 5. CI/CD (GitHub Actions + AWS OIDC)

**Files:** `.github/workflows/`, `infrastructure/modules/github_oidc/`

### `ci.yml`

- Every PR / push: typecheck → unit tests (backend + frontend)
- Push to main: additionally runs the Playwright E2E suite

### `deploy.yml`

- Auto-deploys on every merge to main: Lambda code update → S3 sync → CloudFront invalidation
- **AWS OIDC**: no long-lived `AWS_ACCESS_KEY_ID` in Secrets — uses `sts:AssumeRoleWithWebIdentity`
- `index.html` served with `no-cache`; hashed assets served with `immutable` for optimal CDN caching

### Terraform: `modules/github_oidc/`

- Creates the GitHub Actions OIDC provider in AWS
- Least-privilege IAM role scoped to this repo only
- Run `terraform apply` and copy the `github_actions_role_arn` output into `AWS_DEPLOY_ROLE_ARN` in GitHub Secrets

---

---

# 2026-04-12 추가 변경 (세션 3)

## CI/CD 파이프라인 오류 3건 수정

### 문제 1: SonarCloud — Automatic Analysis 충돌

**원인:** SonarCloud 프로젝트에 Automatic Analysis가 켜진 상태에서 CI 분석도 동시에 실행되어 exit code 3 반환
**해결:** SonarCloud 프로젝트 설정에서 Automatic Analysis 비활성화 (코드 변경 없음)

---

### 문제 2: E2E — `@playwright/test` 모듈 못 찾음

**파일:** `.github/workflows/ci.yml`, `package.json`

**원인 1:** `npm ci --workspace=frontend`가 root `node_modules`를 초기화하고 frontend 의존성만 재설치 → `@playwright/test` 소실

- `.github/workflows/ci.yml`: E2E job의 중복 `npm ci --workspace=frontend` 제거

**원인 2:** `npx playwright test`가 로컬 `node_modules/.bin/playwright`를 무시하고 npx 캐시에서 별도 `playwright` 패키지를 다운받아 실행 → 해당 패키지가 `@playwright/test`를 resolve 못 함

- `package.json`: `"test:e2e": "npx playwright test"` → `"./node_modules/.bin/playwright test"` 변경

---

### 문제 3: Deploy — AWS credentials 로드 실패

**원인:** `AWS_DEPLOY_ROLE_ARN` GitHub Secret 미설정 (`infrastructure/modules/github_oidc/` 모듈이 `terraform apply` 안 된 상태)
**해결:**

1. `terraform init` → `terraform apply` 실행 (3개 리소스 신규 생성)
   - `aws_iam_openid_connect_provider.github`
   - `aws_iam_role.github_actions` (`hdb-cats-github-actions-prod`)
   - `aws_iam_role_policy.deploy`
2. GitHub Secrets에 아래 값 등록

```
AWS_DEPLOY_ROLE_ARN        = xxx
FRONTEND_BUCKET            = xxx
CLOUDFRONT_DISTRIBUTION_ID = xxx
VITE_API_BASE_URL          = xxx
VITE_CLOUDFRONT_DOMAIN     = xxx
```

**최종 결과:** CI (6 E2E passed), Deploy 모두 green

---

---

# 2026-04-12 Additional Changes (Session 3) — English

## CI/CD Pipeline — 3 failures resolved

### Issue 1: SonarCloud — Automatic Analysis conflict

**Cause:** SonarCloud's Automatic Analysis was enabled while CI-based analysis was also running, causing exit code 3
**Fix:** Disabled Automatic Analysis in SonarCloud project settings (no code change)

---

### Issue 2: E2E — `Cannot find module '@playwright/test'`

**Files:** `.github/workflows/ci.yml`, `package.json`

**Root cause 1:** `npm ci --workspace=frontend` wipes root `node_modules` and reinstalls only frontend deps, removing `@playwright/test`

- Removed the redundant `npm ci --workspace=frontend` step from the E2E job

**Root cause 2:** `npx playwright test` bypasses the local `node_modules/.bin/playwright` and downloads a separate `playwright` package from the npx cache, which cannot resolve `@playwright/test` from its own context

- Changed `test:e2e` script from `npx playwright test` → `./node_modules/.bin/playwright test`

---

### Issue 3: Deploy — AWS credentials could not be loaded

**Cause:** `AWS_DEPLOY_ROLE_ARN` GitHub Secret was missing; the `infrastructure/modules/github_oidc/` module had never been applied
**Fix:**

1. Ran `terraform init` then `terraform apply` — created 3 new resources:
   - `aws_iam_openid_connect_provider.github`
   - `aws_iam_role.github_actions` (`hdb-cats-github-actions-prod`)
   - `aws_iam_role_policy.deploy`
2. Registered the following GitHub Secrets:

```
AWS_DEPLOY_ROLE_ARN        = xxx
FRONTEND_BUCKET            = xxx
CLOUDFRONT_DISTRIBUTION_ID = xxx
VITE_API_BASE_URL          = xxx
VITE_CLOUDFRONT_DOMAIN     = xxx
```

**Outcome:** CI (6 E2E passed) and Deploy both green

---

---

# 2026-04-12 추가 변경 (세션 4)

## Multi-environment 인프라 구성 (dev / uat / prod)

### 1. 환경 분리

**파일:** `infrastructure/environments/dev/`, `infrastructure/environments/uat/`, `infrastructure/environments/prod/`

- 동일 AWS 계정 안에서 `dev`, `uat`, `prod` 세 환경으로 분리
- 각 환경마다 별도 tfvars, main.tf, variables.tf, outputs.tf 구성

---

### 2. OIDC Provider 중복 방지

**파일:** `infrastructure/modules/github_oidc/main.tf`, `infrastructure/modules/github_oidc/variables.tf`

- AWS 계정당 동일 URL의 OIDC Provider는 1개만 허용 → `count = var.create_oidc_provider ? 1 : 0` 조건 추가
- prod만 `create_oidc_provider = true`, dev/uat는 `false`로 data source로 참조

---

### 3. Basic Auth (CloudFront Function) — dev 제거, uat 유지

**파일:** `infrastructure/modules/frontend/main.tf`, `infrastructure/modules/frontend/variables.tf`

- `basic_auth_credential` 변수가 비어있으면 CloudFront Function 생성 안 함 (`count = local.enable_basic_auth ? 1 : 0`)
- `dynamic "function_association"` 블록으로 distribution에 조건부 연결
- dev: 초기에 제거했다가 재추가 → Basic Auth 활성 (`https://d3ok4mhx34ddwk.cloudfront.net`)
- uat: `basic_auth_credential` 설정 → Basic Auth 활성

---

### 4. CloudFront Function 삭제 순서 버그 수정

**파일:** `infrastructure/modules/frontend/main.tf`

**문제:** `count`가 1→0으로 바뀔 때 Terraform이 distribution과 function 간 암묵적 의존성을 잃어버려, function이 distribution보다 먼저 삭제 시도 → `FunctionInUse` 에러 발생

**해결:** `aws_cloudfront_distribution` 리소스에 `depends_on = [aws_cloudfront_function.basic_auth]` 추가

- destroy 그래프에서 distribution이 먼저 업데이트(association 제거)되고, 이후 function이 삭제됨
- 현재 stuck 상태 해제 방법: `terraform apply -target=module.frontend.aws_cloudfront_distribution.frontend` → `terraform apply`

---

### 5. 배포 워크플로우 (dev / uat / prod)

**파일:** `.github/workflows/deploy.yml`

- `develop` 브랜치 push → dev 자동 배포
- `main` 브랜치 push → uat 자동 배포 → prod는 수동 승인(GitHub Environments) 후 배포
- 6개 job: `dev-backend`, `dev-frontend`, `uat-backend`, `uat-frontend`, `prod-backend`, `prod-frontend`

---

### 6. Dependabot 브랜치 CI 제외

**파일:** `.github/workflows/ci.yml`

- 모든 job에 `if: github.actor != 'dependabot[bot]'` 조건 추가
- Dependabot PR에서는 CI 빌드 실행 안 함 (minor/patch 의존성 업데이트 자동 머지 지원)

---

### 7. README 업데이트

**파일:** `README.md`

- 멀티 환경 인프라 구조, 브랜치 전략, Basic Auth 설정 방법 추가
- GitHub Secrets 목록 (dev/uat/prod 각각) 문서화

---

---

# 2026-04-12 Additional Changes (Session 4) — English

## Multi-environment Infrastructure (dev / uat / prod)

### 1. Environment separation

**Files:** `infrastructure/environments/dev/`, `infrastructure/environments/uat/`, `infrastructure/environments/prod/`

- Split into three environments within the same AWS account
- Each environment has its own tfvars, main.tf, variables.tf, and outputs.tf

---

### 2. OIDC Provider deduplication

**Files:** `infrastructure/modules/github_oidc/main.tf`, `infrastructure/modules/github_oidc/variables.tf`

- AWS allows only one OIDC provider per URL per account → added `count = var.create_oidc_provider ? 1 : 0`
- Only prod sets `create_oidc_provider = true`; dev and uat reference it via a data source

---

### 3. Basic Auth (CloudFront Function) — removed from dev, kept in uat

**Files:** `infrastructure/modules/frontend/main.tf`, `infrastructure/modules/frontend/variables.tf`

- CloudFront Function is created only when `basic_auth_credential` is non-empty (`count = local.enable_basic_auth ? 1 : 0`)
- `dynamic "function_association"` conditionally attaches it to the distribution
- dev: removed then re-added → Basic Auth active (`https://d3ok4mhx34ddwk.cloudfront.net`)
- uat: `basic_auth_credential` set → Basic Auth active

---

### 4. CloudFront Function deletion ordering bug fix

**File:** `infrastructure/modules/frontend/main.tf`

**Problem:** When `count` transitions from 1 to 0, Terraform loses the implicit dependency between the distribution and the function (the dynamic block is now empty), causing it to attempt deleting the function before the distribution association is removed → `FunctionInUse` error

**Fix:** Added `depends_on = [aws_cloudfront_function.basic_auth]` to `aws_cloudfront_distribution`

- In the destroy graph, the distribution is always updated (association removed) before the function is deleted
- To unstick the current dev state: `terraform apply -target=module.frontend.aws_cloudfront_distribution.frontend` then `terraform apply`

---

### 5. Deployment workflow (dev / uat / prod)

**File:** `.github/workflows/deploy.yml`

- Push to `develop` → auto-deploy to dev
- Push to `main` → auto-deploy to uat → prod requires manual approval via GitHub Environments
- 6 jobs: `dev-backend`, `dev-frontend`, `uat-backend`, `uat-frontend`, `prod-backend`, `prod-frontend`

---

### 6. Exclude Dependabot branches from CI

**File:** `.github/workflows/ci.yml`

- Added `if: github.actor != 'dependabot[bot]'` to all jobs
- CI does not run on Dependabot PRs, enabling auto-merge for minor/patch dependency updates

---

### 7. README update

**File:** `README.md`

- Documented multi-environment infrastructure layout, branch strategy, and Basic Auth setup
- Added GitHub Secrets reference table for each environment (dev / uat / prod)

---

---

# 2026-04-12 추가 변경 (세션 5)

## 1. dev 환경 재구성 — 클라우드 프론트엔드 제거

**파일:** `infrastructure/environments/dev/main.tf`, `outputs.tf`, `variables.tf`, `terraform.tfvars`, `.github/workflows/deploy.yml`, `frontend/.env.local`

- dev 클라우드 환경은 백엔드(Lambda + DynamoDB + API Gateway)만 유지
- dev CloudFront Distribution, S3 프론트엔드 버킷, Basic Auth Function 삭제
- `deploy.yml`에서 `deploy-dev-frontend` job 제거 (`develop` 브랜치는 백엔드만 배포)
- `frontend/.env.local`이 prod를 바라보던 것을 dev 백엔드로 수정
- **이유:** 로컬 개발 시 `npm run dev`로 충분하며, 클라우드 dev 프론트는 역할이 모호하고 비용만 발생

---

## 2. prod 환경 Basic Auth 추가

**파일:** `infrastructure/environments/prod/variables.tf`, `infrastructure/environments/prod/main.tf`

- prod도 `basic_auth_credential` 변수 및 frontend 모듈 인자 추가 (dev/uat와 동일한 구조)
- terraform apply 후 prod CloudFront에도 Basic Auth 적용됨

---

## 3. 고양이 수정(Edit) 기능 추가

### Backend: `updateCat` Lambda (신규)

**파일:** `backend/src/handlers/updateCat.ts`

- `PATCH /cats/{id}` 엔드포인트
- Google OAuth 인증 필수
- DynamoDB에서 기존 항목 조회 후 `userId` 일치 여부 확인 → 불일치 시 403
- 수정 가능 필드: `title`, `description`, `hdbBlock`, `town`
- 빈 문자열로 전송 시 DynamoDB에서 해당 속성 REMOVE (SET/REMOVE 혼합 UpdateExpression)

### Infrastructure

**파일:** `infrastructure/modules/api/main.tf`

- `update_cat` Lambda 함수, integration, route(`PATCH /cats/{id}`), permission 추가
- CORS `allow_methods`에 `PATCH` 추가

### Frontend

**파일:** `frontend/src/api/client.ts`, `frontend/src/hooks/useCats.ts`, `frontend/src/hooks/useSelectedCat.ts`, `frontend/src/components/Sidebar/CatDetailSidebar.tsx`, `frontend/src/App.tsx`

- `client.ts`: `updateCat()` 함수 추가
- `useCats`: `updateCatInList()` 추가 (지도 마커 실시간 반영)
- `useSelectedCat`: `updateSelectedCat()` 추가 (사이드바 실시간 반영)
- `CatDetailSidebar`: 로그인 유저에게 Edit / Delete 버튼 노출
  - Edit 모드: 인라인 폼으로 전환 (title, description, hdbBlock, town)
  - Delete 모드: 2단계 확인 후 삭제

---

## 4. 고양이 삭제(Delete) 인증 개선

**파일:** `backend/src/handlers/deleteCat.ts`

- 기존: admin API key 전용
- 변경: admin key **또는** Google OAuth 소유자 인증 중 하나 허용
  - admin key 우선 확인, 없으면 Google OAuth 토큰 + userId 소유권 검증
- **이유:** 프론트엔드에서 admin key를 노출 없이 본인 게시글 삭제 가능하도록

### Frontend

**파일:** `frontend/src/api/client.ts`

- `deleteCat(id, token)` 시그니처 변경 — `Authorization` 헤더 추가

---

## 5. CI/CD: updateCat Lambda 배포 추가

**파일:** `.github/workflows/deploy.yml`

- 모든 환경(dev/uat/prod) Lambda 배포 루프에 `updateCat` 추가

---

---

# 2026-04-12 Additional Changes (Session 5) — English

## 1. Dev environment restructure — remove cloud frontend

**Files:** `infrastructure/environments/dev/main.tf`, `outputs.tf`, `variables.tf`, `terraform.tfvars`, `.github/workflows/deploy.yml`, `frontend/.env.local`

- dev cloud environment now only hosts backend (Lambda + DynamoDB + API Gateway)
- Removed dev CloudFront distribution, S3 frontend bucket, and Basic Auth function
- Removed `deploy-dev-frontend` job from deploy.yml (`develop` branch deploys backend only)
- Updated `frontend/.env.local` from prod endpoints to dev backend endpoints
- **Rationale:** local `npm run dev` is sufficient for frontend development; a cloud dev frontend adds cost without value

---

## 2. Basic Auth added to prod

**Files:** `infrastructure/environments/prod/variables.tf`, `infrastructure/environments/prod/main.tf`

- Added `basic_auth_credential` variable and wired it into the frontend module (mirrors dev/uat pattern)

---

## 3. Cat edit feature

### Backend: `updateCat` Lambda (new)

**File:** `backend/src/handlers/updateCat.ts`

- `PATCH /cats/{id}` endpoint
- Requires Google OAuth token
- Fetches the existing item and checks `userId` ownership — returns 403 if mismatch
- Editable fields: `title`, `description`, `hdbBlock`, `town`
- Empty string value triggers a DynamoDB `REMOVE` for that attribute (mixed SET/REMOVE expression)

### Infrastructure

**File:** `infrastructure/modules/api/main.tf`

- Added `update_cat` Lambda, integration, route (`PATCH /cats/{id}`), and permission
- Added `PATCH` to CORS `allow_methods`

### Frontend

**Files:** `frontend/src/api/client.ts`, `frontend/src/hooks/useCats.ts`, `frontend/src/hooks/useSelectedCat.ts`, `frontend/src/components/Sidebar/CatDetailSidebar.tsx`, `frontend/src/App.tsx`

- `client.ts`: added `updateCat()` function
- `useCats`: added `updateCatInList()` to reflect edits on map markers instantly
- `useSelectedCat`: added `updateSelectedCat()` to reflect edits in sidebar instantly
- `CatDetailSidebar`: shows Edit / Delete buttons for logged-in users
  - Edit mode: switches to inline form (title, description, hdbBlock, town)
  - Delete mode: two-step confirmation before deleting

---

## 4. Cat delete auth improvement

**File:** `backend/src/handlers/deleteCat.ts`

- Before: admin API key only
- After: accepts either admin key **or** Google OAuth token with ownership check
  - Admin key is checked first; falls back to OAuth + userId validation
- **Rationale:** allows owners to delete their own posts from the frontend without exposing the admin key

### Frontend

**File:** `frontend/src/api/client.ts`

- Updated `deleteCat(id, token)` signature to include `Authorization` header

---

## 5. CI/CD: add updateCat to Lambda deploy

**File:** `.github/workflows/deploy.yml`

- Added `updateCat` to the Lambda deploy loop for all environments (dev / uat / prod)

---

---

# 2026-04-12 추가 변경 (세션 6)

## 좋아요(Like) 기능 추가

### Backend: `toggleLike` Lambda (신규)

**파일:** `backend/src/handlers/toggleLike.ts`

- `POST /cats/{id}/like` — 토글 방식 (이미 좋아요 → 취소, 아니면 → 추가)
- Google OAuth 인증 필수
- Like 항목을 기존 테이블에 저장: `PK=CAT`, `SK=LIKE#{catId}#{userId}`
- 고양이 항목의 `likeCount` 를 DynamoDB ADD 연산으로 원자적 증감
- 응답: `{ likeCount, likedByMe }`

### Backend: `getCat` 업데이트

**파일:** `backend/src/handlers/getCat.ts`

- 응답에 `likeCount` 항상 포함 (미설정 시 0)
- `Authorization` 헤더 있을 경우 해당 유저의 `likedByMe` 추가 반환

### Backend: 타입 업데이트

**파일:** `backend/src/types/index.ts`

- `CatItem`에 `likeCount?: number` 추가
- `CatPublic`에 `likeCount: number`, `likedByMe?: boolean` 추가

### Infrastructure

**파일:** `infrastructure/modules/api/main.tf`

- `toggleLike` Lambda 함수, integration, route(`POST /cats/{id}/like`), permission 추가
- **per-route 좋아요 rate limit**: 초당 10 req, burst 20 (기본 50/100 대비 타이트하게)
- stage에 `depends_on = [aws_apigatewayv2_route.toggle_like]` 추가 (route 생성 전 stage 업데이트 방지)

### Frontend

**파일:** `frontend/src/types/index.ts`, `frontend/src/api/client.ts`, `frontend/src/hooks/useSelectedCat.ts`, `frontend/src/components/Sidebar/CatDetailSidebar.tsx`

- `Cat` 타입에 `likeCount: number`, `likedByMe?: boolean` 추가
- `client.ts`: `fetchCat` 토큰 인자 추가 (로그인 시 `likedByMe` 포함 응답), `toggleLike()` 함수 추가
- `useSelectedCat`: 고양이 조회 시 로그인 토큰 자동 전달
- `CatDetailSidebar`: 상세 정보 하단에 하트 버튼 + 좋아요 수 표시
  - 로그인 전: 카운트만 표시, 클릭 비활성화
  - 로그인 후: 하트 토글 (채워진 하트 ↔ 빈 하트), 클릭 즉시 반영
  - 좋아요/취소 실패 시 silent fail (비핵심 기능)

### CI/CD

**파일:** `.github/workflows/deploy.yml`

- Lambda 배포 루프에 `toggleLike` 추가

---

---

# 2026-04-12 Additional Changes (Session 6) — English

## Like feature

### Backend: `toggleLike` Lambda (new)

**File:** `backend/src/handlers/toggleLike.ts`

- `POST /cats/{id}/like` — toggle: removes like if already liked, adds it otherwise
- Requires Google OAuth token
- Stores like items in the existing table: `PK=CAT`, `SK=LIKE#{catId}#{userId}`
- Atomically increments/decrements `likeCount` on the cat item via DynamoDB ADD
- Returns `{ likeCount, likedByMe }`

### Backend: `getCat` update

**File:** `backend/src/handlers/getCat.ts`

- Always returns `likeCount` (defaults to 0 if unset)
- Returns `likedByMe` if a valid `Authorization` header is present

### Backend: type updates

**File:** `backend/src/types/index.ts`

- Added `likeCount?: number` to `CatItem`
- Added `likeCount: number` and `likedByMe?: boolean` to `CatPublic`

### Infrastructure

**File:** `infrastructure/modules/api/main.tf`

- Added `toggleLike` Lambda, integration, route (`POST /cats/{id}/like`), and permission
- **Per-route rate limit on like toggle**: 10 req/sec, burst 20 (tighter than the default 50/100)
- Added `depends_on = [aws_apigatewayv2_route.toggle_like]` to the stage to prevent it updating before the route exists

### Frontend

**Files:** `frontend/src/types/index.ts`, `frontend/src/api/client.ts`, `frontend/src/hooks/useSelectedCat.ts`, `frontend/src/components/Sidebar/CatDetailSidebar.tsx`

- Added `likeCount: number` and `likedByMe?: boolean` to `Cat` type
- `client.ts`: added optional token arg to `fetchCat` (includes `likedByMe` when authenticated), added `toggleLike()`
- `useSelectedCat`: passes auth token when fetching cat detail
- `CatDetailSidebar`: heart button with like count below the cat metadata
  - Unauthenticated: count shown, button disabled
  - Authenticated: filled/empty heart toggle, updates instantly on click
  - Toggle failures are silent (non-critical feature)
